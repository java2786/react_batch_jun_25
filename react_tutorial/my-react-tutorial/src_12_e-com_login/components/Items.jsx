import { useEffect } from "react";
import { useNavigate } from "react-router";

export default function Items({loggedIn}){
    // const navigate = useNavigate()
    // useEffect(function(){
    //     if(loggedIn==false){
    //         navigate("/login");
    //     }
    // },[]);
    return (
        <div>
            <h1>Items Page</h1>
        </div>
    );
}